using backend.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace backend.Helpers
{
    public class CommentJoinCommentGetAll
    {
        //public System.Linq.IQueryable<CommentGetAll> Convert(System.Linq.IQueryable<Comment> comments, backendContext backend)
        //{
        //        var commentsUsername = new List<CommentGetAll>;

        //        foreach (var comment in comments)
        //        {
        //            CommentGetAll entity = new CommentGetAll();
        //            entity.Id = comment.Id;
        //            entity.Text = comment.Text;
        //            entity.UserId = comment.UserId;
        //            entity.EventId = comment.EventId;

        //            commentsUsername.Append(entity);
        //        }
        //        return commentsUsername;
        //}
    }
}
